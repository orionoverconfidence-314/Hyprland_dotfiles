###################
### KEYBINDINGS ###
###################

$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

## Key bind to kill waybar and run again

bind = CTRL + SHIFT,W , exec, pkill waybar && g++ /home/orion_siva/.config/waybar/scripts/wallpaper_scripts/change_theme.cpp -o /home/orion_siva/.config/waybar/scripts/wallpaper_scripts/change_theme && /home/orion_siva/.config/waybar/scripts/wallpaper_scripts/change_theme && waybar

bind = $mainMod, W, exec, ~/.local/bin/toggle-waybar.sh

##g++ /home/orion_siva/.config/waybar/scripts/wallpaper_sscripts/change_theme.cpp -o /home/orion_siva/.config/waybar/scripts/wallpaper_scripts/change_theme &&/home/orion_siva/.config/waybar/scripts/wallpaper_scripts/change_theme &&

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
#bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod,F,fullscreen

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggless
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Toggle control center
bind = $mainMod SHIFT,n, exec, swaync-client -t -sw

##screenshot by hyprshot
# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -m window

# Screenshot a monitor
bind = , PRINT, exec, hyprshot -m output

# Screenshot a region
 bind = $mainMod SHIFT, PRINT, exec, hyprshot -m region

#cliphist
bind = $mainMod SHIFT, V, exec, /home/orion_siva/.config/rofi/scripts/cliphist_rofi.sh

#rofi -emojis
bind = $mainMod SHIFT, SPACE, exec, rofi -modi emoji -show emoji -theme /home/orion_siva/.config/rofi/themes/rofi-emoji-config.rasi

## lock
bind = SHIFT,ESCAPE , exec , hyprlock

# Cycle wallpapers on Super 
bind = SUPER, I, exec, /home/orion_siva/.config/rofi/scripts/cycle_wallpaper.sh

bind = SUPER SHIFT, I, exec, /home/orion_siva/.config/rofi/scripts/rofi-wallpaper.sh

# wlogout
bind = CTRL,ESCAPE, exec, wlogout -b 6

# Hyprexpo

bind = $mainMod, X, hyprexpo:expo, toggle

#--------------------------------------------------------#

## submap is super powerful

# bind=ALT,R,submap,resize
# submap=resize
# bind=,right,resizeactive,10 0
# bind=,left,resizeactive,-10 0
# bind=,up,resizeactive,0 -10
# bind=,down,resizeactive,0 10
# bind=,escape,submap,reset
# submap=reset


## Screeshot
#bBBind = PRINT, exec, grim -g "$(slurp)" ~/Pictures/screenshot.png

